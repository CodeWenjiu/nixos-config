- match
sudo nixos-rebuild switch --flake .#wenjiu

- vscode server
```bash
systemctl --user enable auto-fix-vscode-server.service # auto
systemctl --user start auto-fix-vscode-server.service # enable
```

